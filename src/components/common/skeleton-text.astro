---
import { cn } from '@utils/index';

interface Props {
	count?: number;
	class?: any;
}

const { count = 1, class: className } = Astro.props;
---

{
	Array.from({ length: count }).map((_) => (
		<div class={cn('mb-2 h-3 w-full rounded-lg bg-gray-200', className)} />
	))
}
